<%= form_for(@role) do |f| %>
  <% if @role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@role.errors.count, "error") %> prohibited this role from being saved:</h2>

      <ul>
      <% @role.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
	
	<%@store_modules.each do |sm|%>
		 <%=check_box_tag "module_permission[module_permission_id][]", sm.id,@role.store_modules.map(&:name).include?(sm.name)%><%=sm.name%>
	<%end%>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
