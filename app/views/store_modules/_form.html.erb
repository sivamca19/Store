<%= form_for(@store_module) do |f| %>
  <% if @store_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@store_module.errors.count, "error") %> prohibited this store_module from being saved:</h2>

      <ul>
      <% @store_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
